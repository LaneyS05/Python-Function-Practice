import converter

print(converter.kg_to_lbs(70))

# below is a better way to import moduels
from converter import kg_to_lbs
from utils import find_max

numbers = [10, 3, 6, 2]


print(kg_to_lbs(100))
max_number = find_max(numbers)
print(max_number)
